DESCRIPTION="SDL memory game"
HOMEPAGE="http://lgames.sourceforge.net/"
SRC_URI="mirror://sourceforge/lgames/${P}.tar.gz"

DIFF_EXCLUDES="acinclude.m4"

src_compile() {
	cd ${S}
	rm -f acinclude.m4
	cygautoreconf

	cd ${B}
	cygconf
	cygmake
}
